language: python
python:
#  - "3.4"
  - "2.7"
  - "2.6"
before_install:
  - sudo apt-get install unixodbc-dev
  - sudo apt-get install libsqliteodbc
  - ls /usr/lib
install:
  - locate libsqliteodbc.so
  - pip install -e .
script:
  - if [[ $TRAVIS_PYTHON_VERSION == 2* ]]; then python tests2/sqlitetests.py 'Driver=/usr/lib/libsqliteodbc.so;Database=sqlite.db'; fi
  - if [[ $TRAVIS_PYTHON_VERSION == 3* ]]; then python tests3/sqlitetests.py 'Driver=/usr/lib/libsqliteodbc.so;Database=sqlite.db'; fi
